using UnityEngine;
using System.Collections;

public class GodBehaviour : MonoBehaviour {
	
	// Use this for initialization
	void Start () {
		//GameManager.Instance.vrRootNode.scale = 10.0f;
	}
	
	// Update is called once per frame
	void Update () {
	
	}

	void FixedUpdate () {
		//God.Instance.vrRootNode.transform = 10.0f * God.MainCamera.Instance.vrRootNode.transform
	}
}
